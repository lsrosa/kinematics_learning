r3_arm:

  # Publish all joint states
  joint_state_controller:
    type: joint_state_controller/JointStateController
    publish_rate: 50  


  # Position controller
  joint_group_position_controller:
    type: "effort_controllers/JointGroupPositionController"
    joints:
      - joint_1
      - joint_2
      - joint_3
    joint_1:
      pid: {p: 0.7, i: 0, d: 0.0001 }
    joint_2:
      pid: {p: 0.7, i: 0, d: 0.0001 }
    joint_3:
      pid: {p: 0.7, i: 0, d: 0.0001 }
      
      
  # Trajectory controller
  joint_trajectory_controller:
    type: "effort_controllers/JointTrajectoryController"
    joints:
      - joint_1
      - joint_2
      - joint_3

    constraints:
      goal_time: 0.5                   # Override default
      stopped_velocity_tolerance: 0.02 # Override default
      joint_1:
        trajectory: 0.05               # Not enforced if unspecified
        goal: 0.01                     # Not enforced if unspecified
      joint_2:
        trajectory: 0.05               # Not enforced if unspecified
        goal: 0.01                     # Not enforced if unspecified
      joint_3:
        trajectory: 0.05               # Not enforced if unspecified
        goal: 0.01                     # Not enforced if unspecified

    gains: # Required because this is position control using an effort interface
      joint_1: {p: 1, i: 0, d: 0, i_clamp: 1}
      joint_2: {p: 1, i: 0, d: 0, i_clamp: 1}
      joint_3: {p: 1, i: 0, d: 0, i_clamp: 1}

    state_publish_rate:  50            # Override default
    action_monitor_rate: 50            # Override default
    stop_trajectory_duration: 0        # Override default


  # Effort controller
  joint_group_effort_controller:
    type: "effort_controllers/JointGroupEffortController"
    joints:
      - joint_1
      - joint_2
      - joint_3




# check controllers type
# rosservice call /r4_arm/controller_manager/list_controller_types
# sudo apt install ros-noetic-ros-control ros-noetic-ros-controllers 




